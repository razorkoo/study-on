{% extends 'base.html.twig' %}

{% block title %}Lesson{% endblock %}

{% block body %}
    <h1>{{ lesson.title }}</h1>

    <p>Курс <a href="{{ path('course_show', {id: lesson.lessonCourse.id}) }}">{{course.title}}</a></p>
    <div>
        <a class="btn btn-secondary" href="{{ path('lesson_edit', {'id': lesson.id}) }}">Редактировать</a>

        {{ include('lesson/_delete_form.html.twig') }}
    </div>
    <div class="card mt-3">
        <div class="card-body">
            {{ lesson.content }}
        </div>
    </div>

    {% endblock %}
